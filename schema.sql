CREATE TABLE IF NOT EXISTS nodes_moisture_data (date, time, node_id, moisture);

CREATE TABLE IF NOT EXISTS nodes_watering_data (date, time, node_id, last_watered);

CREATE TABLE IF NOT EXISTS sys_sensor_data (date, time, temperature, humidity);

